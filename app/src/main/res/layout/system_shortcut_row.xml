<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/system_shortcut_item"
    android:layout_width="@dimen/bg_popup_item_width"
    android:layout_height="wrap_content"
    android:minHeight="@dimen/bg_popup_item_height"
    android:foreground="@drawable/popup_item_ripple"
    android:paddingStart="@dimen/popup_padding_start"
    android:paddingEnd="@dimen/popup_padding_end"
    android:gravity="center_vertical">

    <TextView
        android:id="@+id/shortcut_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="start|center_vertical"
        android:minHeight="@dimen/bg_popup_item_height"
        android:textAlignment="viewStart"
        android:paddingStart="@dimen/deep_shortcuts_text_padding_start"
        android:paddingEnd="@dimen/popup_padding_end"
        android:textSize="14sp"
        android:lines="1"
        android:ellipsize="end"
        android:textColor="?attr/popupTextColor" />

    <ImageView
        android:id="@+id/shortcut_icon"
        android:layout_width="@dimen/system_shortcut_icon_size"
        android:layout_height="@dimen/system_shortcut_icon_size"
        android:layout_marginStart="@dimen/system_shortcut_margin_start"
        android:layout_gravity="start|center_vertical"
        android:tint="?attr/popupTextColor" />

</FrameLayout>
